<section class="section contact">
  <div class="container">
    <h2>Contact</h2>
    {% if site.ENABLE_SECTION_DESCRIPTIONS %}
    <p class="section-description">{{ site.SECTION_DESCRIPTIONS.contact }}</p>
    {% endif %}
    <div class="contact-info">
      <div class="contact-item">
        <strong>📍 Adresse</strong>
        <p>{{ site.ADDRESS }}</p>
      </div>
      
      <div class="contact-item">
        <strong>📞 Téléphone</strong>
        <p><a href="tel:{{ site.PHONE_TEL }}">{{ site.PHONE_PRETTY }}</a></p>
      </div>
      
      <div class="contact-item">
        <strong>✉️ Email</strong>
        <p><a href="mailto:{{ site.EMAIL }}">{{ site.EMAIL }}</a></p>
      </div>
      
      {% if site.MAPS_URL %}
      <div class="contact-item">
        <a href="{{ site.MAPS_URL }}" target="_blank" class="maps-link">
          🗺️ Voir sur Google Maps
        </a>
      </div>
      {% endif %}
    </div>
  </div>
</section>
