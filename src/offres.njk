---
layout: base.njk
title: Choisissez votre offre
permalink: /offres/
---

<style>
  body {
    background: #0a0a0a;
  }
  
  .offers-page {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }
  
  .navbar {
    background: rgba(26,26,26,0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid #2a2a2a;
    padding: 1rem 1.5rem;
    margin-bottom: 2rem;
    position: sticky;
    top: 0;
    z-index: 100;
  }
  
  .navbar-content {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }
  
  .navbar-brand {
    color: #E8B4B8;
    font-size: 1.5rem;
    font-weight: 900;
    letter-spacing: -0.02em;
  }
  
  .navbar-tagline {
    color: #999;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-align: right;
  }
  
  .hero-offers {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .hero-offers h1 {
    font-size: 2rem;
    font-weight: 800;
    margin-bottom: 0.75rem;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: #FFFFFF;
    text-transform: uppercase;
  }
  
  .hero-offers .subtitle {
    font-size: 1rem;
    color: #999;
    font-weight: 500;
  }
  
  .offers-container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .offer-box {
    background: #1a1a1a;
    border: 2px solid #2a2a2a;
    border-radius: 4px;
    padding: 2.5rem 2rem;
    transition: all 0.2s ease;
  }
  
  .offer-box:hover {
    border-color: #E8B4B8;
    box-shadow: 0 8px 24px rgba(232,180,184,0.15);
    transform: translateY(-2px);
  }
  
  .offer-box.featured {
    border: 2px solid #E8B4B8;
    background: #1a1a1a;
    box-shadow: 
      0 20px 60px rgba(232,180,184,0.25),
      0 0 0 1px #E8B4B8,
      inset 0 0 30px rgba(232,180,184,0.05);
    transform: scale(1.02);
  }
  
  .offer-box.featured:hover {
    transform: scale(1.02) translateY(-2px);
    box-shadow: 
      0 30px 80px rgba(232,180,184,0.35),
      0 0 0 1px #E8B4B8,
      inset 0 0 40px rgba(232,180,184,0.08);
  }
  
  .offer-badge {
    background: #2a2a2a;
    color: #E8B4B8;
    padding: 0.35rem 0.85rem;
    border-radius: 2px;
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    display: inline-block;
    margin-bottom: 1.5rem;
    border: 1px solid #E8B4B8;
  }
  
  .offer-box.featured .offer-badge {
    background: #E8B4B8;
    color: #000;
    font-weight: 800;
    border: none;
  }
  
  .offer-title {
    font-size: 1.75rem;
    font-weight: 800;
    margin-bottom: 1rem;
    color: #FFFFFF;
    text-transform: uppercase;
    letter-spacing: -0.02em;
  }
  
  .offer-price {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 1rem;
    line-height: 1;
    color: #FFFFFF;
  }
  
  .offer-box.featured .offer-title {
    color: #E8B4B8;
    font-weight: 800;
  }
  
  .offer-box.featured .offer-price {
    font-size: 4.5rem;
    background: linear-gradient(135deg, #E8B4B8 0%, #D4A590 50%, #E8B4B8 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 900;
    letter-spacing: -0.03em;
    margin-bottom: 0.5rem;
  }
  
  .offer-box.featured .offer-price-period {
    color: #E8B4B8;
    font-weight: 600;
  }
  
  .offer-price-period {
    font-size: 1.25rem;
    color: #999;
    font-weight: 600;
  }
  
  .offer-desc {
    color: #999;
    margin-bottom: 2rem;
    line-height: 1.6;
  }
  
  .offer-box.featured .offer-desc {
    color: #bbb;
    font-weight: 500;
  }
  
  .offer-features {
    list-style: none;
    margin-bottom: 2rem;
  }
  
  .offer-features li {
    padding: 0.625rem 0;
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    font-size: 0.9rem;
    color: #CCCCCC;
    line-height: 1.5;
    border-bottom: 1px solid rgba(255,255,255,0.08);
  }
  
  .offer-features li:last-child {
    border-bottom: none;
  }
  
  .offer-features li::before {
    content: '✓';
    color: #E8B4B8;
    font-size: 1rem;
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 2px;
  }
  
  .offer-features li.disabled {
    color: #999;
    opacity: 0.5;
  }
  
  .offer-features li.disabled::before {
    content: '×';
    color: #ccc;
  }
  
  .offer-cta {
    display: block;
    width: 100%;
    background: linear-gradient(135deg, #E8B4B8 0%, #D4A590 100%);
    color: white;
    text-align: center;
    padding: 1.125rem 2rem;
    border-radius: 10px;
    text-decoration: none;
    font-weight: 700;
    font-size: 1rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 6px 20px rgba(232,180,184,0.35);
    border: none;
    position: relative;
    overflow: hidden;
  }
  
  .offer-cta::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255,255,255,0.3);
    transform: translate(-50%, -50%);
    transition: width 0.5s, height 0.5s;
  }
  
  .offer-cta:hover::before {
    width: 300px;
    height: 300px;
  }
  
  .offer-cta:hover {
    background: linear-gradient(135deg, #D4A590 0%, #C89F8D 100%);
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 10px 30px rgba(232,180,184,0.5);
  }
  
  .offer-box.featured .offer-cta {
    background: linear-gradient(135deg, #D4A590 0%, #C89F8D 100%);
    box-shadow: 0 8px 25px rgba(212,165,144,0.45);
    font-weight: 800;
  }
  
  .offer-box.featured .offer-cta:hover {
    background: linear-gradient(135deg, #C89F8D 0%, #B8956A 100%);
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 35px rgba(212,165,144,0.6);
  }
  
  @media (min-width: 768px) {
    .offers-page { padding: 3rem 2rem; }
    .navbar-brand { font-size: 1.75rem; }
    .navbar-tagline { font-size: 0.8rem; }
    .hero-offers h1 { font-size: 2.75rem; }
  }
  
  @media (min-width: 1024px) {
    .offers-page { padding: 4rem 2rem; }
    .navbar-brand { font-size: 2rem; }
    .navbar-tagline { font-size: 0.9rem; }
    .hero-offers h1 { font-size: 3.5rem; }
    .offers-container { grid-template-columns: repeat(3, 1fr); }
  }
</style>

<nav class="navbar">
  <div class="navbar-content">
    <div class="navbar-brand">.tekNa</div>
    <div class="navbar-tagline">Sites vitrines qui convertissent</div>
  </div>
</nav>

<div class="offers-page">
  <div class="hero-offers">
    <h1>Choisissez votre offre</h1>
    <p class="subtitle">Des sites qui convertissent, livrés en 7 jours</p>
  </div>
  
  <div class="offers-container">
    {% for key, offer in offers %}
    <div class="offer-box {% if offer.featured %}featured{% endif %}">
      {% if offer.featured %}
      <div class="offer-badge">Recommandé</div>
      {% endif %}
      
      <h3 class="offer-title">{{ offer.name }}</h3>
      
      <div class="offer-price">
        {{ offer.price }}<span class="offer-price-period">€</span>
      </div>
      
      <p class="offer-desc">{{ offer.description }}</p>
      
      <ul class="offer-features">
        {% for feature in offer.features %}
        <li {% if not feature.included %}class="disabled"{% endif %}>
          {{ feature.name }}
        </li>
        {% endfor %}
      </ul>
      
      <a href="/previews/{{ offer.id }}/" class="offer-cta" target="_blank">
        Voir le site
      </a>
    </div>
    {% endfor %}
  </div>
</div>
