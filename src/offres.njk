---
layout: base.njk
title: Choisissez votre offre
permalink: /offres/
---

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;900&display=swap" rel="stylesheet">

<style>
  body {
    background: #FFFFFF;
  }
  
  .offers-page {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }
  
  .navbar {
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid #eee;
    padding: 1rem 1.5rem;
    margin-bottom: 2rem;
    position: sticky;
    top: 0;
    z-index: 100;
  }
  
  .navbar-content {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }
  
  .navbar-brand {
    color: #F99395;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    font-size: 1.5rem;
    font-weight: 900;
    letter-spacing: -0.02em;
  }
  
  .navbar-tagline {
    color: #666;
    font-size: 0.7rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-align: right;
  }
  
  .hero-offers {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .hero-offers h1 {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: #0F0F0F;
    text-transform: uppercase;
  }
  
  .hero-offers .subtitle {
    font-size: 1rem;
    color: #666;
    font-weight: 400;
  }
  
  .offers-container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .offer-box {
    background: #FFFFFF;
    border: 1px solid #EEF1F4;
    border-radius: 30px;
    padding: 2.5rem 2rem;
    transition: all 0.2s ease;
  }
  
  .offer-box:hover {
    border-color: #F99395;
    box-shadow: 0 8px 24px rgba(249,147,149,0.15);
    transform: translateY(-2px);
  }
  
  .offer-box.featured {
    border: 2px solid #F99395;
    background: linear-gradient(135deg, #FFFFFF 0%, #FFF9FA 100%);
    box-shadow: 0 20px 60px rgba(249,147,149,0.2);
    transform: scale(1.02);
  }
  
  .offer-box.featured:hover {
    transform: scale(1.02) translateY(-2px);
    box-shadow: 0 30px 80px rgba(249,147,149,0.3);
  }
  
  .offer-badge {
    background: #EEF1F4;
    color: #0F0F0F;
    padding: 0.35rem 0.85rem;
    border-radius: 20px;
    font-size: 0.65rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    display: inline-block;
    margin-bottom: 1.5rem;
  }
  
  .offer-box.featured .offer-badge {
    background: #F99395;
    color: #FFFFFF;
    font-weight: 700;
  }
  
  .offer-title {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #0F0F0F;
    text-transform: uppercase;
    letter-spacing: -0.02em;
  }
  
  .offer-price {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 1rem;
    line-height: 1;
    color: #0F0F0F;
  }
  
  .offer-box.featured .offer-title {
    color: #F99395;
    font-weight: 800;
  }
  
  .offer-box.featured .offer-price {
    font-size: 4.5rem;
    background: linear-gradient(135deg, #F99395 0%, #F5A4A6 50%, #F99395 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-weight: 900;
    letter-spacing: -0.03em;
    margin-bottom: 0.5rem;
  }
  
  .offer-box.featured .offer-price-period {
    color: #F99395;
    font-weight: 600;
  }
  
  .offer-price-period {
    font-size: 1.25rem;
    color: #999;
    font-weight: 600;
  }
  
  .offer-desc {
    color: #666;
    margin-bottom: 2rem;
    line-height: 1.6;
  }
  
  .offer-box.featured .offer-desc {
    color: #141414;
    font-weight: 400;
  }
  
  .offer-features {
    list-style: none;
    margin-bottom: 2rem;
  }
  
  .offer-features li {
    padding: 0.625rem 0;
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    font-size: 0.9rem;
    color: #141414;
    line-height: 1.5;
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }
  
  .offer-features li:last-child {
    border-bottom: none;
  }
  
  .offer-features li::before {
    content: '✓';
    color: #F99395;
    font-size: 1rem;
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 2px;
  }
  
  .offer-features li.disabled {
    color: #999;
    opacity: 0.5;
  }
  
  .offer-features li.disabled::before {
    content: '×';
    color: #ddd;
  }
  
  .offer-cta {
    display: block;
    width: 100%;
    background: #0F0F0F;
    color: white;
    text-align: center;
    padding: 1.125rem 2rem;
    border-radius: 30px;
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.2s ease;
    border: none;
  }
  
  .offer-cta:hover {
    background: #F99395;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(249,147,149,0.3);
  }
  
  .offer-box.featured .offer-cta {
    background: #F99395;
    font-weight: 700;
  }
  
  .offer-box.featured .offer-cta:hover {
    background: #F5A4A6;
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(249,147,149,0.4);
  }
  
  @media (min-width: 768px) {
    .offers-page { padding: 3rem 2rem; }
    .navbar-brand { font-size: 1.75rem; }
    .navbar-tagline { font-size: 0.8rem; }
    .hero-offers h1 { font-size: 2.75rem; }
  }
  
  @media (min-width: 1024px) {
    .offers-page { padding: 4rem 2rem; }
    .navbar-brand { font-size: 2rem; }
    .navbar-tagline { font-size: 0.9rem; }
    .hero-offers h1 { font-size: 3.5rem; }
    .offers-container { grid-template-columns: repeat(3, 1fr); }
  }
</style>

<nav class="navbar">
  <div class="navbar-content">
    <div class="navbar-brand">.tekNa</div>
    <div class="navbar-tagline">Sites vitrines qui convertissent</div>
  </div>
</nav>

<div class="offers-page">
  <div class="hero-offers">
    <h1>Choisissez votre offre</h1>
    <p class="subtitle">Des sites qui convertissent, livrés en 7 jours</p>
  </div>
  
  <div class="offers-container">
    {% for key, offer in offers %}
    <div class="offer-box {% if offer.featured %}featured{% endif %}">
      {% if offer.featured %}
      <div class="offer-badge">Recommandé</div>
      {% endif %}
      
      <h3 class="offer-title">{{ offer.name }}</h3>
      
      <div class="offer-price">
        {{ offer.price }}<span class="offer-price-period">€</span>
      </div>
      
      <p class="offer-desc">{{ offer.description }}</p>
      
      <ul class="offer-features">
        {% for feature in offer.features %}
        <li {% if not feature.included %}class="disabled"{% endif %}>
          {{ feature.name }}
        </li>
        {% endfor %}
      </ul>
      
      <a href="/previews/{{ offer.id }}/" class="offer-cta" target="_blank">
        Voir le site
      </a>
    </div>
    {% endfor %}
  </div>
</div>
